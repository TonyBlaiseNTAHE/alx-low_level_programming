#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints its name, followed by a new line.
 * @argc: contains the number of arguments passed to the program
 * @argv: arrays size of argc
 * Return: no return
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
